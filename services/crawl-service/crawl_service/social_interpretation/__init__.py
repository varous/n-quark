"""Social interpretation (Phase 5C.2) — derived, versioned, deterministic interpretation of immutable
social evidence, and governed projection into the EXISTING reconciliation machinery.

Layering (never collapsed):
    SocialMention (immutable observed evidence)
      → SocialInterpretation (derived deterministic interpretation of ONE evidence version)
        → EventMatchCandidate (a hypothesis in the existing reconciliation surface)
          → Canonical Event (registry/reconciliation-owned — NEVER created by this path)

No LLM anywhere in this path; classification is pure/deterministic and exposes why each label fired.
"""
